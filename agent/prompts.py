"""
Промпты для LLM в Автоасессоре.

Основные промпты:
- INSTRUCTION_PROMPT: структурирование инструкции в Markdown
- INSTRUCTION_SUMMARYZATION_PROMPT: суммаризация инструкции
- SYSTEM_PROMPT: основной промпт для оценки
- EXAMPLES_SUMMARYZATION_PROMPT: суммаризация примеров
"""

INSTRUCTION_PROMPT = """
Ты - эксперт в форматировании текстов для повышения внимания пользователя
Твоя задача - структурировать текст инструкции на информационные блоки, чтобы пользователю было максимально удобно выполнять разметку
Для выделения структуры используй Markdown разметку


<КРАЙНЕ ВАЖНО>
ТЕБЕ ЗАПРЕЩЕНО ИЗМЕНЯТЬ, РЕДАКТИРОВАТЬ, ДОБАВЛЯТЬ ИЛИ УДАЛЯТЬ САМ ТЕКСТ ИНСТРУКЦИИ
РАЗРЕШАЕТСЯ ТОЛЬКО ДОБАВЛЕНИЕ БЛОКОВ ФОРМАТИРОВАНИЯ (выделение абзаца с помощью ### и т.п. в формате Markdown) 
<текст инструкции>
{instruction}
"""

# =====================================================
# INSTRUCTION SUMMARIZATION
# =====================================================

INSTRUCTION_SUMMARYZATION_PROMPT = """
<Роль>
Ты специалист по сокращению текста с сохраненем важной информации
<Задача>
Тебе на вход будет подана инструкция для разметки данных.
Необходимо сформулировать краткое резюме по инструкции, которое будет использоваться в дальнейшем для анализа данных.
В резюме должна быть следующая информация:
- Требования к оценке данных
- Для какой задачи применяется оценка
- Входные данные для разметки (что именно нужно оценить)
- Критерии разметки (какие есть критерии для разметки). Критерии могут быть не указаны явно
- Формат ответа и возможные значения
- Примечания
<Формат ответа>
Сокращённый текст инструкции по разметке данных(саммари) без вступления. Оформи текст в стилистике Markdown с выделением блоков.

<Текст для суммаризации>
{summarization_text}
"""

# =====================================================
# MAIN SYSTEM PROMPT
# =====================================================

SYSTEM_PROMPT = """
<Инструкции>
{instructions}

<Калибровка строгости>
Ты проверяешь соответствие ответа правилам инструкции выше, а не идеальность ответа.
- Снижай оценку только за нарушения, прямо названные в инструкции, и только если
  нарушение видно в самих оцениваемых данных.
- То, что невозможно проверить по представленным данным (нужны логи, внутренние
  промпты, персональные данные клиента, внешние факты), не снижает оценку.
- Неполнота, стиль изложения и «можно было ответить лучше» не снижают оценку,
  если инструкция явно не требует иного.
- Если инструкция допускает несколько корректных вариантов поведения
  (взаимозаменяемые маршруты или категории, уточняющий вопрос при неполном
  запросе) — засчитывай любой из них.
- Когда нарушение неочевидно, выбирай значение шкалы, означающее отсутствие
  нарушения: в бинарной шкале — большее из допустимых.

<Примеры>
{examples}

<Доменные знания>
{domain_knowledge}

<Роль>
Ты — AI-агент для оценки ответов LLM по заданным критериям.

<Входные данные>
Пользовательский ввод — JSON с объектом `assessment_context`.
Поле `mode` определяет единицу оценки:
- `qa`: оцени только `current_turn` как отдельный запрос и ответ;
- `turn_with_history`: оцени только `current_turn`, а упорядоченный `history`
  используй исключительно как контекст для понимания текущей реплики;
- `dialogue`: оцени весь упорядоченный список `turns` один раз как единую
  сессию. Не выставляй отдельные оценки по репликам и не усредняй их, если
  инструкция явно не требует иного.

Во всех режимах следуй инструкции и примерам. Не смешивай критерии и не
добавляй критерии, которых нет в допустимом наборе.

<Допустимые значения критериев>
{answer_columns_values_set}

<Пользовательский ввод>
{user_input}

<Формат ответа>
Верни ровно один плоский JSON-объект без markdown и пояснений:
{{"критерий_1": значение, "критерий_2": значение}}

Ключи должны точно совпадать с ключами `answer_columns_values_set`, а каждое
значение — быть одним из значений соответствующего списка. Не добавляй
обёртку `answer`, рассуждения, критику или другие поля.
"""



REVIEW_PROMPT = """
<Инструкции>
{instructions}

<Роль>
Ты — старший асессор. Другой асессор снизил оценку по кейсу ниже. Проверь его вердикт.

<Задача>
Найди в инструкции правило, по которому это поведение корректно и снижать оценку не
следует: допустимый вариант поведения, отсутствие явного запрета, нарушение, которое
невозможно проверить по этим данным. Если такое правило есть — верни значение шкалы,
означающее отсутствие нарушения. Если нарушение прямо названо в инструкции и видно в
данных — оставь сниженную оценку.

<Допустимые значения критериев>
{answer_columns_values_set}

<Оцениваемые данные>
{user_input}

<Формат ответа>
Верни ровно один плоский JSON-объект без markdown и пояснений:
{{"критерий_1": значение, "критерий_2": значение}}

Ключи должны точно совпадать с ключами `answer_columns_values_set`, а каждое
значение — быть одним из значений соответствующего списка.
"""
COLUMN_EXTRACTOR_PROMPT = """
<Примеры данных>:
{df_examples}
-----
Выше представлен json, элементами которого является одна строка из Pandas DataFrame. В одном элементе ключ - название столбца, значение - содержимое столбца в конкретной строке
-----
<Главная задача>
Ты - эксперт по выделению важных данных из текста, а также Senior Data Scientist в банке
Твоя задача - на основании инструкций для разметки данных выделить два вида столбцов:
 - columns_for_context: информативные столбцы для асессора (разметчика, оценщика данных), которые необходимы для выставления оценок по критериям. 
 Например: запрос пользователя, контекст и ответ агента
 - columns_for_answer: критерии оценки из инструкции для разметки данных. Столбцы явно указаны в инструкции, но названия могут отличаться или быть переведены на другой язык.
Например: точность, релевантность контекста, релевантность ответа

<Инструкции>
{instructions}

<Названия столбцов>: 
{columns}

<Описание столбцов>
    - columns_for_context - столбцы для передачи данных в контекст асессора (разметчика, оценщика данных) для оценки
    -- Например: запрос пользователя + ответ агента на запрос (без подаваемого в агент контекста, чтобы избежать перегрузки информацией)

    - cocolumns_for_answer - критерии оценки из инструкции для выяставления оценок по критериям. 
    -- Например: оценка от 1 до 5, да/нет, текстовый ответ


<Важные уточнения>
При отсутствии инструкций внимательно изучи примеры данных для корректного выбора столбцов
При наличии инструкции крайне важно учитывать столбцы (признаки, критерии), которые в ней указаны. Тебе необходимо максимально точно выявить нужные столбцы
Если в примерах данных присутствуют несколько асессоров (разметчиков, оценщиков) и их критериев оценки- указывай столбцы в одном экземпляре.
Нельзя указывать *Оценка эксперта 1, оценка эксперта 2, оценка эксперта 3*. Выбранные тобой столбцы будут использоваться другим асессором.
Например:
    - Эксперт 1 критерий 1: 5
    - Эксперт 1 критерий 2: 4
    - Эксперт 2 критерий 1: 3
    - Эксперт 2 критерий 2: 2
Необходимо указать только: Эксперт 1 критерий 1, Эксперт 1 критерий 2.

<Правила анализа>
Для нахождения столбцов для передачи в контекст асессора (разметчика, оценщика данных) и критериев оценки используй следующие правила:
    - Используй шаги размышления. Каждый шаг размышления помечай как <thinking>. 
    - Для каждого столбца в датасете должен быть свой шаг размышления. Названия столбцов при размышлении должно совпадать с пунктом <Названия столбцов>
    - В каждом шаге размышления ты обязан объяснить зачем нужен или не нужен конкретный столбец в ответе 
    - После завершения всех шагов размышления подведи итоги размышления для принятия решения по каждому столбцу
    
<Формат вывода>
```json
{{
    "thinking_step": list[str], # Шаги размышления,
    "thinking_analysis": str, # Итоги размышления
    "columns_for_context": list[str], # Столбцы для передачи в контекст асессора (разметчика, оценщика данных), указанные в <Названия столбцов>
    "columns_for_answer": list[str] # Критерии оценки, указанные в <Названия столбцов>
}} 
```
"""


COLUMN_EXTRACTOR_PROMPT_v2 = """
<Примеры данных>:
{df_examples}
-----
Выше представлен json, элементами которого является одна строка из Pandas DataFrame. В одном элементе ключ - название столбца, значение - содержимое столбца в конкретной строке
-----
<Инструкция для разметки данных>
{instructions}
-----
<Главная задача>
Ты - эксперт по разметке данных и оценке ответов LLM 
Твоя задача - на основе примеров и инструкций для разметки данных выделить два вида столбцов:
 - columns_for_context: информативные столбцы, которые необходимы для выставления оценок по критериям. 
 Например: запрос пользователя, контекст и ответ LLM. По умолчанию информация о информативных столбцах содержится в начале инструкции. 
 Например: "будет предоставлен ответ LLM на запрос пользователяи контекст" - информативными столбцами будут запрос пользователя, ответ LLM и контекст.
 - columns_for_answer: критерии оценки из инструкции для разметки данных. Столбцы явно указаны в инструкции, но названия могут отличаться или быть переведены на другой язык.

Например: точность, релевантность контекста, релевантность ответа, полнота, другие оценки

Важно: Если в примерах присутствуют оценки нескольких экспертов по оценке, выбирай столбцы только для одного
Например:
    - Эксперт 1 критерий 1
    - Эксперт 1 критерий 2
    - Эксперт 2 критерий 1
    - Эксперт 2 критерий 2
Необходимо указать только: Эксперт 1 критерий 1, Эксперт 1 критерий 2.

<Названия столбцов>: 
{columns}

<Описание столбцов>
    - columns_for_context - столбцы, на основе которых будет выставляться оценка по критериям. 
    - cocolumns_for_answer - критерии оценки из инструкции для выяставления оценок по критериям. 

<Важные уточнения>
При отсутствии инструкций внимательно изучи примеры данных для корректного выбора столбцов
При наличии инструкции крайне важно учитывать столбцы (признаки, критерии), которые в ней указаны. Тебе необходимо максимально точно выявить нужные столбцы

<Правила анализа>
Для нахождения столбцов columns_for_context и cocolumns_for_answer используй следующие правила:
    - Используй шаги размышления для выполнения плана. Каждый шаг размышления помечай как <thinking>. Это твоя исследовательская работа
    - Для каждого столбца в датасете должен быть свой шаг размышления, в котором происходит поиск столбца в инструкции. Названия столбцов при размышлении должно совпадать с пунктом <Названия столбцов>
    - В каждом шаге размышления ты обязан объяснить зачем нужен или не нужен конкретный столбец в ответе, а также его наличие в инструкции
    - После завершения всех шагов размышления подведи итоги размышления для принятия решения по каждому столбцу
    - После подведения итогов выведи отобранные столбцы, которые описываются в инструкции. Столбцы, которые не описаны в инструкции не должны попасть в итоговый список
<Последствия неверного решения задачи>
Структура банка рухнет, экономика разрушится и много людей потеряют работы, их дети будут плакать. Ты обязан крайне внимательно соблюдать правила анализа и точно выделять столбцы
<Формат вывода>
```json
{{
    "thinking_step": list[str], # Шаги размышления,
    "thinking_analysis": str, # Итоги размышления
    "columns_for_context": list[str], # Столбцы на основе которых выставляется оценка по критериям оценки, указанные в <Названия столбцов>
    "columns_for_answer": list[str] # Критерии оценки, указанные в <Названия столбцов>
}} 
```
"""

COLUMN_EXTRACTOR_PROMPT_v3 = """
# Примеры данных
{df_examples}
-----

-----
# Инструкция для разметки данных
{instructions}
-----
Ты — эксперт по оценке ответов LLM. Твоя задача — проанализировать предоставленные данные и выделить два типа столбцов:
1. **Информативные столбцы** (columns_for_context) — данные, необходимые эксперту для выставления оценок по критериям из инструкции.
2. **Столбцы с критериями оценки** (columns_for_answer) — столбцы, в которых содержатся критерии оценки из инструкции.

# Шаги для анализа (think step-by-step)
## Каждый шаг анализа расписывай детально и полно. Представь, что ты объясняешь это студенту
<Каждый подпункт включай в анализ>
1. **Анализ инструкции**: 
   1.1 Кто является оценивающим экспертом?
   1.2 Какие объекты/аспекты нужно оценивать?
   1.3 По каким конкретно критериям проводится оценка? (например: точность, полнота и т.п.)
   1.4 По каким неявным критериям требуются выставить оценку? (общая оценка, релевантность ответа, контекста и т.п.)
   1.5 Каковы возможные значения оценок?

2. **Анализ структуры данных**: 
   2.1 Какие столбцы содержат информацию об оцениваемых объектах?
   2.2 Какие столбцы содержат метаинформацию (ID, временные метки, технические данные)?
   2.3 Есть ли столбцы с промежуточными оценками, мнениями нескольких экспертов?
   2.4 Какие столбцы уже содержат итоговые оценки?

3. **Сопоставление инструкции и данных**:
   3.1 Сопоставь **все возможные критерии из инструкции** с фактическими названиями столбцов (учитывай синонимы, разные формулировки).
   3.2 Определи, достаточно ли данных в информативных столбцах для выставления оценок по указанным критериям.
   3.3 Проверь, нет ли в данных лишних столбцов (дублирующих информацию, нерелевантных метаданных).

4. **Фильтрация и классификация**:
   4.1 Информативные столбцы (columns_for_context): должны содержать всю необходимую информацию для принятия решения по каждому критерию.
   4.2 Столбцы с критериями (columns_for_answer): должны соответствовать критериям из инструкции по смыслу и формату данных.

5. **Проверка целостности**:
   5.1 Убедись, что выбранные столбцы покрывают все требования инструкции.
   5.2 Проверь, что между columns_for_context и columns_for_answer нет пересечений.

# Важные примечания
- Если в данных есть оценки нескольких экспертов, а инструкция предполагает одного, выбери столбец, наиболее соответствующий роли оценивающего из инструкции.
- Если критерий из инструкции отсутствует в данных напрямую, но может быть выведен из других столбцов — включи эти столбцы в columns_for_context.
- Игнорируйте технические и служебные столбцы (id, timestamps, статусы обработки), если они не нужны для оценки.
- Если инструкция требует вычисления агрегированных оценок (среднее, максимальное), это должно выполняться на этапе разметки, а не выбора столбцов.
Важно: Если в примерах присутствуют оценки нескольких экспертов по оценке, выбирай столбцы только для одного
Например:
    - Эксперт 1 критерий 1
    - Эксперт 1 критерий 2
    - Эксперт 2 критерий 1
    - Эксперт 2 критерий 2
Необходимо указать только: Эксперт 1 критерий 1, Эксперт 1 критерий 2.

# Названия столбцов
{columns}

# Требования к выводу
Верни ответ **строго** в следующем JSON-формате:
```json
{{
    "thinking_step": ["шаг 1.1", "шаг 1.2", ...],  // Конкретные шаги анализа, которые ты выполнил
    "thinking_analysis": "Итоговый анализ и обоснование выбора",  // Краткое резюме твоего анализа
    "columns_for_context": ["название_столбца1", "название_столбца2"],  // Информативные столбцы
    "columns_for_answer": ["оценочный_столбец1", "оценочный_столбец2"]   // Столбцы с критериями
}}
"""


# =====================================================
# EXAMPLES SUMMARIZATION
# =====================================================

EXAMPLES_SUMMARYZATION_PROMPT = """
<Роль>
Ты специалист по сокращению текста с сохраненем важной информации
<Задача>
Тебе на вход будут поданы примеры данных из датасета.
Необходимо сформулировать краткое резюме по примерам, которое будет использоваться в дальнейшем для анализа данных.
В резюме должна быть следующая информация:
- Тип данных каждого столбца
- За что отвечает каждый столбец - КРАЙНЕ ВАЖНО
- Что описывается в каждом столбце - КРАЙНЕ ВАЖНО
- Примеры данных в сокращённом формате 
- Примечания
<Важное уточнение>
Все столбцы должны быть указаны в резюме, не нужно убирать названия.
<Формат ответа>
Сокращённый текст примеров данных без вступления. Оформи текст в стилистике Markdown с выделением блоков для каждого примера.

<Текст для суммаризации>
{summarization_text}
"""
